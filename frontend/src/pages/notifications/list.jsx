import {useState} from 'react';
import {Link} from 'react-router-dom';
import {notificationsApi} from '../../api/api';
import useResource from '../../hooks/useResource';
import {ErrorAlert} from '../../components/Common';
import ConfirmDialog from '../../components/ConfirmDialog';
export default function Notifications(){const [page,setPage]=useState(0),[error,setError]=useState(null),[busy,setBusy]=useState(false),[clearOpen,setClearOpen]=useState(false),[success,setSuccess]=useState('');const r=useResource(async()=>({inbox:await notificationsApi.list(page),summary:await notificationsApi.summary()}),[page]);async function action(fn,message){setBusy(true);setError(null);try{await fn();setSuccess(message);setClearOpen(false);r.reload();window.dispatchEvent(new Event('charity-notifications-changed'))}catch(e){setError(e)}finally{setBusy(false)}}const d=r.data?.inbox;return <div className="container py-4 notification-inbox"><div className="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><h1 className="mb-1">Notifications</h1><p className="text-secondary mb-0">Your task assignments and upcoming events, all in one place.</p></div><div className="d-flex gap-2">{r.data?.summary.unread>0&&<button className="btn btn-outline-primary" disabled={busy} onClick={()=>action(notificationsApi.readAll,'All notifications marked as read.')}><i className="bi bi-check2-all me-1"/>Mark all as read</button>}{(d?.totalElements??d?.content?.length)>0&&<button className="btn btn-outline-danger" onClick={()=>setClearOpen(true)}><i className="bi bi-trash me-1"/>Clear all notifications</button>}</div></div><ErrorAlert error={error||r.error}/>{success&&<div className="alert alert-success" role="status">{success}</div>}{clearOpen&&<ConfirmDialog title="Clear all notifications?" message="This clears all read and unread notifications from your inbox and header. You will still receive new notifications and emails." confirmLabel="Clear all notifications" busy={busy} onCancel={()=>setClearOpen(false)} onConfirm={()=>action(async()=>{await notificationsApi.clear();setPage(0)},'Notifications cleared.')}/>} {r.loading?<p role="status">Loading notifications…</p>:d&&(!d.content.length?<div className="empty-state"><i className="bi bi-bell display-6"/><h2 className="h5 mt-3">You're all caught up</h2><p className="text-secondary mb-0">New task assignments and event reminders will appear here.</p></div>:d.content.map(i=><article className={`card notification-card p-4 mb-3 ${i.unread?'is-unread':''}`} id={`notification-${i.id}`} key={i.id}><div className="d-flex align-items-start gap-3"><span className="notification-icon"><i className="bi bi-bell"/></span><div className="flex-grow-1 notification-copy"><div className="d-flex flex-wrap align-items-center gap-2 mb-2"><h2 className="h5 mb-0">{i.title}</h2>{i.unread&&<span className="badge rounded-pill text-bg-primary">New</span>}</div><p className="notification-message text-secondary">{i.message}</p><div className="d-flex flex-wrap gap-3 align-items-center">{i.eventPath&&<Link className="btn btn-sm btn-outline-primary" to={i.eventPath}>View event</Link>}{i.unread&&<button className="btn btn-sm btn-outline-secondary" disabled={busy} onClick={()=>action(()=>notificationsApi.read(i.id),'Notification marked as read.')}>Mark as read</button>}<time className="small text-secondary">{i.createdAt?new Date(i.createdAt).toLocaleString():''}</time></div></div></div></article>))}{d?.totalPages>1&&<nav className="d-flex justify-content-between mt-4" aria-label="Notification pages"><button className="btn btn-outline-secondary" disabled={page===0} onClick={()=>setPage(page-1)}>Newer</button><span className="text-secondary">Page {page+1} of {d.totalPages}</span><button className="btn btn-outline-secondary" disabled={page+1>=d.totalPages} onClick={()=>setPage(page+1)}>Older</button></nav>}</div>}
